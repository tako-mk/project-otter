04/21
    FastAPIでプロジェクト作成
    仮想環境の構築

    python3 -m venv venv
    Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass 
    source venv/bin/activate           
    pip install fastapi uvicorn  

04/28
    studentサーバーにて環境構築
    一旦sqliteで作って後でpostgresにする

    pip install sqlalchemy

05/12
    lukeにてライブラリインストールに苦戦
    pipのバージョンが古いようでうまくいかなかった
    アップデートをしようといろいろな方法を試したが
    proxyにひっかかって上手く行かなかった
    最終的にget-pip.pyをdlして実行することで強制的にpipを新しくし、
    ライブラリをインストールすることができた

    pip install fastapi uvicorn sqlalchemy

06/02
    仮にsqliteでプロジェクト直下にdata/app.dbを作成
    仮想環境で uvicorn app.main:app --reloadを実行し
    ローカルホストで動いていることを確認した
    sqlalchemyはpython上で動くsqlライブラリ
    SQLiteやPostgresのそれぞれの違いを吸収できる
    
    main.pyでfastapi起動
    database.pyでデータベースの初期設定
    models/user.pyでユーザーテーブルの構造
    schemas/user.pyでapiが受け取る側のデータを設定